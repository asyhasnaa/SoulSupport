<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Base.Theme.SoulSupport" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:textColor">@color/primary_600</item>
    </style>

    <style name="Widget.App.ExtendedFloatingActionButton" parent="Widget.Material3.ExtendedFloatingActionButton.Icon.Primary">
        <item name="materialThemeOverlay">@style/ThemeOverlay.App.FloatingActionButton</item>
    </style>

    <style name="Widget.App.FloatingActionButton" parent="Widget.Material3.FloatingActionButton.Primary">
        <item name="materialThemeOverlay">@style/ThemeOverlay.App.FloatingActionButton</item>
    </style>

    <style name="ThemeOverlay.App.FloatingActionButton" parent="">
        <item name="colorContainer">@color/primary_100</item>
        <item name="colorOnContainer">@color/secondary_500</item>
    </style>

    <style name="Theme.SoulSupport" parent="Base.Theme.SoulSupport" />

    <!-- Styling Switch Night Mode -->
    <style name="nightModeSwitch" parent="Theme.AppCompat.Light">
        <item name="colorControlActivated">@color/primary_900</item>
        <item name="colorSwitchThumbNormal">@color/primary_900</item>
        <item name="android:colorForeground">@color/primary_900</item>
    </style>

    <!-- Styling Switch Notification -->
    <style name="notificationSwitch" parent="Theme.AppCompat.Light">
        <item name="colorControlActivated">@color/secondary_800</item>
        <item name="colorSwitchThumbNormal">@color/secondary_800</item>
        <item name="android:colorForeground">@color/secondary_800</item>
    </style>

    <!-- Styling Indicator Bottom Navigation -->
    <style name="App.Custom.Indicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/primary_500</item>
    </style>

    <style name="ShapeAppearanceOverlay.Fab.Circle" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>
</resources>